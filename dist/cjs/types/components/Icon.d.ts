import { GeneratedPropTypes } from '../types';
declare type IconProps = {
    icon: string;
    size?: number | number[];
} & GeneratedPropTypes;
export declare const Icon: {
    (props: IconProps): any;
    defaultProps: {
        size: number;
    };
};
export {};
